# ── Core — compatível com Python 3.13 (sem compilação Rust/MSVC) ──────────────
fastapi
uvicorn[standard]
python-multipart
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv

# ── HTTP / Async ───────────────────────────────────────────────────────────────
aiohttp>=3.9
httpx>=0.27
requests>=2.31

# ── AI / LLM ───────────────────────────────────────────────────────────────────
openai>=1.3
anthropic>=0.20
google-generativeai>=0.5

# ── Crypto / Auth ──────────────────────────────────────────────────────────────
cryptography>=41
pycryptodome>=3.19
python-jose>=3.3
passlib>=1.7

# ── Solana / Web3 (opcional — comentar se der conflito) ───────────────────────
# solana e solders têm builds pra Python 3.12 mas não 3.13 ainda
# solders>=0.21
# solana>=0.34
base58>=2.1

# ── Database / Memory (opcional) ──────────────────────────────────────────────
# chromadb>=0.5     # pesado, instalar separado se precisar
redis>=5.0
sqlalchemy>=2.0
