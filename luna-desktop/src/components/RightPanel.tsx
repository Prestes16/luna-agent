// RightPanel.tsx — substituído pelo ExecutionPanel.tsx na V4
// Este arquivo é mantido apenas para compatibilidade com imports legados.
import React from 'react'

/** @deprecated Use ExecutionPanel instead */
const RightPanel: React.FC = () => null

export default RightPanel
